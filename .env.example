# Database (PostgreSQL - matches docker-compose.yml)
DATABASE_URL=postgresql://icc:icc123@localhost:5644/icc-cms

# Security - generate a strong secret (min 32 characters)
# Example: openssl rand -base64 32
PAYLOAD_SECRET=your-super-secret-key-min-32-characters

# Server URL (no trailing slash)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Optional: Cron job authentication
CRON_SECRET=your-cron-secret

# Optional: Database pool settings
# DATABASE_POOL_MAX=10
# DATABASE_POOL_MIN=0
# DATABASE_POOL_IDLE_TIMEOUT=10000

# ======================
# Storage Configuration
# ======================
# Options: "minio" (local/S3) or "vercel" (Vercel Blob)
# If not set, files are stored locally in public/media/
PAYLOAD_STORAGE=minio

# MinIO/S3 storage (when PAYLOAD_STORAGE=minio - matches docker-compose.yml)
MINIO_ENDPOINT=http://localhost:19111
MINIO_REGION=us-east-1
MINIO_BUCKET=icc-dev
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Vercel Blob storage (when PAYLOAD_STORAGE=vercel)
# BLOB_READ_WRITE_TOKEN=your-vercel-blob-token

TODO: 

- remove the 'tenant-viewer' role and allowPublicRead on /admin to make it all credentials.
- fix soft-delete to only enable super-admin to allow actual deletion.
- assess the /(frontend) to only for connection check and gate to /admin (remove other FE component related).
- create .md for agents related to TenantContext, slug, and BE guideline on how FE should fetch. (related to /api/tenants/[slug]/...)
